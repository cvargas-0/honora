# @honora/types

TypeScript type definitions and Zod schemas for [Honora](https://www.npmjs.com/package/create-honora).

This package is used internally by `create-honora` and `@honora/generator`. It exposes the core types that describe the `schema.json` contract, generator configuration, and the ORM adapter interface.
