# @honora/generator

Code generation engine for [Honora](https://www.npmjs.com/package/create-honora).

This package is used internally by `create-honora`. It takes a parsed `schema.json` and produces all the source files for a complete Hono REST API project — routes, database schema, client, migrations config, and utilities — for both Drizzle ORM and Prisma.
