import { {{{imports}}} } from "{{{drizzlePackage}}}";

{{#each collections}}
export const {{name}} = {{../tableConstructor}}("{{name}}", {
  id: {{{idExpression}}},
  created_at: {{{../createdAtExpr}}},
  updated_at: {{{../updatedAtExpr}}},
{{#each columns}}
  {{name}}: {{{expression}}},
{{/each}}
});

{{/each}}
