import { serve } from "@hono/node-server";
import app from "./routes/index{{ext lang}}";

const port = parseInt(process.env.PORT || "3000", 10);
console.log(`Server running on http://localhost:${port}`);
serve({ fetch: app.fetch, port });
