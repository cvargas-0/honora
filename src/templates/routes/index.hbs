import { Hono } from "hono";
{{#each collections}}
import {{name}} from "./{{name}}{{ext ../lang}}";
{{/each}}

export default new Hono()
    .basePath("/api")
    .get("/_health", (c) => c.json({ status: "ok" }))
{{#each collections}}
    .route("/{{name}}", {{name}})
{{/each}}

