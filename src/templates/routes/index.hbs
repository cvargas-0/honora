import { Hono } from "hono";
{{#each collections}}
import {{name}} from "./{{name}}{{#if (eq ../lang "js")}}.js{{/if}}";
{{/each}}

const app = new Hono().get("/api/_health", (c) => c.json({ status: "ok" }))

export default app;
