import { Hono } from "hono";
{{#each collections}}
import {{name}} from "./{{name}}{{#if (eq ../lang "js")}}.js{{/if}}";
{{/each}}

export default new Hono()
    .get("/api/_health", (c) => c.json({ status: "ok" }))
{{#each collections}}
    .route("/{{name}}", {{name}})
{{/each}}

