import { defineConfig } from "drizzle-kit";

export default defineConfig({
  schema: "./src/db/schema.{{#if (eq lang "ts")}}ts{{else}}js{{/if}}",
  out: "./drizzle",
  dialect: "sqlite",
  dbCredentials: {
    url: {{{json dbUrl}}},
  },
});
